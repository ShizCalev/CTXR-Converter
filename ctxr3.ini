[cli]
; Default output format when converting a .ctxr from the command line, drag-and-drop, and file association.
; DXT5-compressed textures (mgs3's leaves) can only be exported as dds

; supports: dds, png, tga

output_format = dds
